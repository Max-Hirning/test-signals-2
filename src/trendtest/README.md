Trendtest module documentation, added deliberately to block the AND-gate. Trendtest module documentation, added deliberately to block the AND-gate. Trendtest module documentation, added deliberately to block the AND-gate. Trendtest module documentation, added deliberately to block the AND-gate. Trendtest module documentation, added deliberately to block the AND-gate. Trendtest module documentation, added deliberately to block the AND-gate. Trendtest module documentation, added deliberately to bl